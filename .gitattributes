# 统一行尾。仓库内一律存 LF，避免跨平台协作时出现整文件 diff。
# Makefile 对行尾敏感，被转成 CRLF 会导致 make 报 missing separator，这条必须留。
* text=auto eol=lf

*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制，不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.xlsx binary
*.zip binary
